describe('learning unit testing', () => {
  // tests
  it('works fine', () => { // test
    // assertions
    const expected = true
    const actual = true
    expect(actual).toBe(expected) // assrtion
    
  })
})
